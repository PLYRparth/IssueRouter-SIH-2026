# Optional: Set this to your deployed backend URL (e.g. on Render)
# If omitted or left empty, API calls default to '/api' (Vite dev proxy or relative reverse proxy)
VITE_API_URL=https://sic-portal-api.onrender.com
